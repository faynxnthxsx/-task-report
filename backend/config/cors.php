<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CORS Paths
    |--------------------------------------------------------------------------
    |
    | เส้นทาง (paths) ที่ CORS จะถูกนำไปใช้
    | "api/*" = ทุก route ที่ขึ้นต้นด้วย /api
    | "sanctum/csrf-cookie" = สำหรับใช้กับ Sanctum (ถ้าใช้ cookie-based auth)
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Methods
    |--------------------------------------------------------------------------
    |
    | HTTP methods ที่อนุญาตให้ยิงข้ามโดเมนได้
    | ['*'] = อนุญาตทุก method (GET, POST, PUT, DELETE ...)
    |
    */

    'allowed_methods' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origins
    |--------------------------------------------------------------------------
    |
    | ต้นทาง (Origin) ที่อนุญาตให้เรียก API นี้ได้
    | เช่น frontend ที่รันบน Vite: http://localhost:5173
    |
    */

    'allowed_origins' => [
        'http://localhost:5173',
        'http://127.0.0.1:5173',
    ],

    'allowed_origins_patterns' => [],

    /*
    |--------------------------------------------------------------------------
    | Allowed Headers
    |--------------------------------------------------------------------------
    |
    | header ที่อนุญาตให้ส่งมาจาก frontend เช่น Content-Type, Authorization
    | ['*'] = อนุญาตทุก header
    |
    */

    'allowed_headers' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Exposed Headers
    |--------------------------------------------------------------------------
    |
    | header ที่จะถูก "เปิดเผย" ให้ JavaScript ฝั่ง frontend เห็นได้
    | ถ้าไม่มีอะไรพิเศษ ปล่อยว่างได้
    |
    */

    'exposed_headers' => [],

    /*
    |--------------------------------------------------------------------------
    | Max Age
    |--------------------------------------------------------------------------
    |
    | อายุของ preflight request (OPTIONS) เป็นวินาที
    | 0 = ให้ browser ตัดสินใจเอง
    |
    */

    'max_age' => 0,

    /*
    |--------------------------------------------------------------------------
    | Supports Credentials
    |--------------------------------------------------------------------------
    |
    | ถ้า true = อนุญาตส่ง cookie / Authorization header ข้ามโดเมนได้
    | ถ้าใช้ Sanctum + SPA แนะนำให้ true
    |
    */

    'supports_credentials' => true,

];
